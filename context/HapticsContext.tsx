import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HapticsContext = () => {
  return (
    <View>
      <Text>HapticsContext</Text>
    </View>
  );
};

export default HapticsContext;

const styles = StyleSheet.create({});
